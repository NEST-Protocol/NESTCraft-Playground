export default function Home() {
  return (
    <main className={`flex justify-center items-center h-screen`}>
      <a href={'/playground'} className={`text-2xl text-red-500 font-bold`}>NESTcraft Playground</a>
    </main>
  )
}
